<div class="notification">
	<div class="notification-header">
		<h3 style="color: #f59e0b; font-size: 1.5rem; line-height: 2rem; font-weight: bold">{{ tournamentName }}</h3>
	</div>
	<div class="notification-body">
		<p>
			{{ 'Tournament has started'|trans }}.
			<a class="notif-link" href="$mainUrl$?table={{ tableId }}" target="_blank">{{'Перейти'|trans}}</a>
		</p>
	</div>
</div>
